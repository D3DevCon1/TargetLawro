<h1>Listing gameweeks</h1>

<table>
  <thead>
    <th>Gameweek</th>
    <th>Date</th>
    <th>Deadline</th>
    <th>Fixtures</th>
  </thead>

<% @gameweeks.each do |gameweek| %>
  <tr>
    <td><%= link_to gameweek.gameweek_no, gameweek %></td>
    <td><%= gameweek.start_date.strftime("%d/%m/%Y") %></td>
	<td><%= gameweek.start_time.strftime("%H:%M") %></td>
    <td><%= gameweek.fixtures %></td>
  </tr>
<% end %>
</table>

<br />

